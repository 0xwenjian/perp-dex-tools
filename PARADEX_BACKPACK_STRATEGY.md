# Paradex-Backpack 对冲策略实现详情

本文档提供了 `hedge_mode_bp_paradex.py` 脚本的全面技术概述，包括其逻辑、功能和安全机制。

## 1. 策略概述

该策略是一种跨交易所对冲，旨在产生交易量的同时最小化市场风险。
- **主要交易所 (挂单方/Maker):** Backpack (BP) - 使用限价单 (Limit orders)。
- **对冲交易所 (吃单方/Taker):** Paradex (PD) - 使用市价单 (Market orders)。

### 核心工作流程
1. **初始同步**：同步两个交易所的持仓。
2. **对齐 (Reconciliation)**：确保双边达到完美对冲状态（即仓位数量相等、方向相反；若同为 0 则视为已对齐）。
3. **Maker 入场 (Backpack)**：在 BP 以最优买入/卖出价挂单。等待20秒，确保订单成交。
4. **Taker 对冲 (Paradex)**：一旦 BP 订单成交（通过 WebSocket 实时追踪），立即在 Paradex 下达等量反向市价单。
5. **周期报表**：在每次双边成交完成后，计算并报告 PnL (盈亏) 和手续费。
6. **循环执行**：重复上述过程，直到达到目标交易量或最大迭代次数。

---

## 2. 关键功能

### 📡 实时 Telegram 通知
- **基于事件驱动**：
  - **周期完成**：详细罗列成交价格、数量以及当轮动态盈亏。
  - **致命错误**：如果仓位无法自动对齐或发生关键错误，立即发出警报。
  - **爆仓预警**：账户风险升高时主动发出警报。
- **定时概览**：每 **2 小时**发送一次持仓更新，以确认机器人仍处于活跃且健康的状态。

### 🚨 主动爆仓监控
- **自动获取**：定期获取 Backpack 和 Paradex 的最新爆仓价格。
- **风险预警**：如果市场价格进入任一交易所爆仓价的 **10%** 范围，将发送高优先级 Telegram 警报。

### 🔧 智能仓位对齐
自动处理因部分成交或延迟导致的仓位差异（残余碎单）：
- **Paradex 优先**：尝试在 Paradex 补齐差额。
- **Backpack 补底**：如果差额太小（低于 Paradex 的最小下单限额/Min Notional），机器人会自动在 **Backpack** 进行反向交易来“抹除”碎单，恢复完美的对冲状态。
- 确保机器人不会因交易所拒单而陷入死循环。

### 📈 精准盈亏 (PnL) 与手续费追踪
- **价格盈亏 (Price PnL)**：计算下单周期内捕获的套利价差。
- **手续费处理**：所有手续费（Backpack Maker 费和 Paradex Taker 费）都被视为负成本，并从净盈亏 (Net PnL) 中扣除。
- **累计追踪**：持续追踪整个交易会话期间的总盈亏和总手续费。

---

## 3. 配置与参数

- `--size`: 每笔交易的数量。
- `--iter`: 总交易循环次数。
- `--sleep`: 完成一轮交易后的持仓等待时长（秒）。
- `--ticker`: 交易对（如 AVAX, HYPE, ETH）。

---

## 4. 安全机制

- **WS 自动重连**：实时监控 Backpack WebSocket 状态。
- **错误重试**：针对 API 调用实现退避重试逻辑，应对网络波动。

---

## 5. 使用示例

```bash
# 示例：AVAX 对冲，每轮 72 个，运行 2 轮，每轮持仓等待约 13 小时
python3 hedge_mode.py --exchange backpack_paradex --ticker AVAX --size 72 --iter 2 --sleep 46800
```
